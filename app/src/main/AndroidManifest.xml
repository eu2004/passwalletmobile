<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ro.group305.passwalletandroidclient">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OpenPassWalletActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ManagePassWalletActivity"
            android:parentActivityName=".OpenPassWalletActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".CreatePassWalletActivity"
            android:parentActivityName=".OpenPassWalletActivity" />
        <activity
            android:name=".EditPassWalletItemActivity"
            android:parentActivityName=".ManagePassWalletActivity" />
        <activity
            android:name=".AddPassWalletItemActivity"
            android:parentActivityName=".ManagePassWalletActivity" />
    </application>

</manifest>